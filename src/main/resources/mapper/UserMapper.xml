<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bible.bible_typing.mapper.UserMapper">

    <select id="countByUserId" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM user_info
        WHERE user_id = #{userId}
    </select>

    <select id="findByUserId" parameterType="string" resultType="com.bible.bible_typing.dto.UserInfoDto">
        SELECT
            user_idx,
            user_id,
            user_pw,
            user_nm,
            user_nic,
            user_email,
            user_birth,
            user_phone,
            use_yn,
            ins_idx,
            ins_date,
            upt_idx,
            upt_date
        FROM user_info
        WHERE user_id = #{userId}
    </select>

    <insert id="insertUser" parameterType="com.bible.bible_typing.dto.UserInfoDto">
        INSERT INTO user_info (
            user_id,
            user_pw,
            user_nm,
            user_nic,
            user_email,
            user_birth,
            user_phone,
            use_yn,
            ins_date
        ) VALUES (
            #{userId},
            #{userPw},
            #{userNm},
            #{userNic},
            #{userEmail},
            #{userBirth},
            #{userPhone},
            'Y',
            NOW()
        )
    </insert>

    <!-- 타자 연습 후 최고 타수 업데이트 (단/장문 공통)-->
    <update id="updateMaxSpeedIfGreater">
        UPDATE user_info
        SET
        <choose>
            <when test="practiceType == 'short'">
                max_speed_short = GREATEST(IFNULL(max_speed_short, 0), #{currentSpeed})
            </when>
            <when test="practiceType == 'long'">
                max_speed_long = GREATEST(IFNULL(max_speed_long, 0), #{currentSpeed})
            </when>
        </choose>
        , upt_idx = #{userIdx}
        , upt_date = NOW()
        WHERE user_id = #{userId}
    </update>
</mapper>